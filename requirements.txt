# MISO_POI_Analysis — Python dependencies
# Install with: pip install -r requirements.txt

# Data collection & storage
requests>=2.31.0
pandas>=2.1.0
pyarrow>=14.0.0       # Parquet read/write backend
ijson>=3.2.0          # streaming JSON parse — keeps MISO-wide API response off the heap

# Cloudflare R2 (S3-compatible object storage)
boto3>=1.34.0
python-dotenv>=1.0.0  # loads .env in local dev

# Scheduling (local dev only — Render Cron handles this in production)
schedule>=1.2.0

# Analysis
numpy>=1.26.0

# Chart generation → static HTML for GitHub Pages
plotly>=5.18.0
